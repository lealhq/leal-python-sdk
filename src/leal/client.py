# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import httpx
from .core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from .core.logging import LogConfig, Logger
from .environment import LealEnvironment

if typing.TYPE_CHECKING:
    from .cards.client import AsyncCardsClient, CardsClient
    from .customer_cards.client import AsyncCustomerCardsClient, CustomerCardsClient
    from .customers.client import AsyncCustomersClient, CustomersClient
    from .locations.client import AsyncLocationsClient, LocationsClient
    from .posters.client import AsyncPostersClient, PostersClient
    from .rewards.client import AsyncRewardsClient, RewardsClient
    from .status.client import AsyncStatusClient, StatusClient
    from .stores.client import AsyncStoresClient, StoresClient


class Leal:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    environment : LealEnvironment
        The environment to use for requests from the client. from .environment import LealEnvironment



        Defaults to LealEnvironment.PRODUCTION



    token : typing.Union[str, typing.Callable[[], str]]
    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    max_retries : typing.Optional[int]
        The default maximum number of retries for failed requests. Defaults to 2. Per-request `max_retries` in `request_options` takes precedence over this value.

    stream_reconnection_enabled : typing.Optional[bool]
        Whether to automatically reconnect on stream disconnection for resumable streaming endpoints. Defaults to True. Per-request `stream_reconnection_enabled` in `request_options` takes precedence over this value.

    max_stream_reconnection_attempts : typing.Optional[int]
        The maximum number of reconnection attempts for resumable streaming endpoints. Defaults to no limit. Per-request `max_stream_reconnection_attempts` in `request_options` takes precedence over this value.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.Client]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    logging : typing.Optional[typing.Union[LogConfig, Logger]]
        Configure logging for the SDK. Accepts a LogConfig dict with 'level' (debug/info/warn/error), 'logger' (custom logger implementation), and 'silent' (boolean, defaults to True) fields. You can also pass a pre-configured Logger instance.

    Examples
    --------
    from leal import Leal

    client = Leal(
        token="YOUR_TOKEN",
    )
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: LealEnvironment = LealEnvironment.PRODUCTION,
        token: typing.Union[str, typing.Callable[[], str]],
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        stream_reconnection_enabled: typing.Optional[bool] = None,
        max_stream_reconnection_attempts: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.Client] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
    ):
        _defaulted_timeout = timeout if timeout is not None else 60 if httpx_client is None else None
        _defaulted_max_retries = max_retries if max_retries is not None else 2
        self._client_wrapper = SyncClientWrapper(
            base_url=_get_base_url(base_url=base_url, environment=environment),
            token=token,
            headers=headers,
            httpx_client=httpx_client
            if httpx_client is not None
            else httpx.Client(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
            if follow_redirects is not None
            else httpx.Client(timeout=_defaulted_timeout),
            timeout=_defaulted_timeout,
            max_retries=_defaulted_max_retries,
            stream_reconnection_enabled=stream_reconnection_enabled,
            max_stream_reconnection_attempts=max_stream_reconnection_attempts,
            logging=logging,
        )
        self._stores: typing.Optional[StoresClient] = None
        self._cards: typing.Optional[CardsClient] = None
        self._customers: typing.Optional[CustomersClient] = None
        self._customer_cards: typing.Optional[CustomerCardsClient] = None
        self._locations: typing.Optional[LocationsClient] = None
        self._posters: typing.Optional[PostersClient] = None
        self._rewards: typing.Optional[RewardsClient] = None
        self._status: typing.Optional[StatusClient] = None

    @property
    def stores(self):
        if self._stores is None:
            from .stores.client import StoresClient  # noqa: E402

            self._stores = StoresClient(client_wrapper=self._client_wrapper)
        return self._stores

    @property
    def cards(self):
        if self._cards is None:
            from .cards.client import CardsClient  # noqa: E402

            self._cards = CardsClient(client_wrapper=self._client_wrapper)
        return self._cards

    @property
    def customers(self):
        if self._customers is None:
            from .customers.client import CustomersClient  # noqa: E402

            self._customers = CustomersClient(client_wrapper=self._client_wrapper)
        return self._customers

    @property
    def customer_cards(self):
        if self._customer_cards is None:
            from .customer_cards.client import CustomerCardsClient  # noqa: E402

            self._customer_cards = CustomerCardsClient(client_wrapper=self._client_wrapper)
        return self._customer_cards

    @property
    def locations(self):
        if self._locations is None:
            from .locations.client import LocationsClient  # noqa: E402

            self._locations = LocationsClient(client_wrapper=self._client_wrapper)
        return self._locations

    @property
    def posters(self):
        if self._posters is None:
            from .posters.client import PostersClient  # noqa: E402

            self._posters = PostersClient(client_wrapper=self._client_wrapper)
        return self._posters

    @property
    def rewards(self):
        if self._rewards is None:
            from .rewards.client import RewardsClient  # noqa: E402

            self._rewards = RewardsClient(client_wrapper=self._client_wrapper)
        return self._rewards

    @property
    def status(self):
        if self._status is None:
            from .status.client import StatusClient  # noqa: E402

            self._status = StatusClient(client_wrapper=self._client_wrapper)
        return self._status


def _make_default_async_client(
    timeout: typing.Optional[float],
    follow_redirects: typing.Optional[bool],
) -> httpx.AsyncClient:
    try:
        import httpx_aiohttp  # type: ignore[import-not-found]
    except ImportError:
        pass
    else:
        if follow_redirects is not None:
            return httpx_aiohttp.HttpxAiohttpClient(timeout=timeout, follow_redirects=follow_redirects)
        return httpx_aiohttp.HttpxAiohttpClient(timeout=timeout)

    if follow_redirects is not None:
        return httpx.AsyncClient(timeout=timeout, follow_redirects=follow_redirects)
    return httpx.AsyncClient(timeout=timeout)


class AsyncLeal:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    environment : LealEnvironment
        The environment to use for requests from the client. from .environment import LealEnvironment



        Defaults to LealEnvironment.PRODUCTION



    token : typing.Union[str, typing.Callable[[], str]]
    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    async_token : typing.Optional[typing.Callable[[], typing.Awaitable[str]]]
        An async callable that returns a bearer token. Use this when token acquisition involves async I/O (e.g., refreshing tokens via an async HTTP client). When provided, this is used instead of the synchronous token for async requests.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    max_retries : typing.Optional[int]
        The default maximum number of retries for failed requests. Defaults to 2. Per-request `max_retries` in `request_options` takes precedence over this value.

    stream_reconnection_enabled : typing.Optional[bool]
        Whether to automatically reconnect on stream disconnection for resumable streaming endpoints. Defaults to True. Per-request `stream_reconnection_enabled` in `request_options` takes precedence over this value.

    max_stream_reconnection_attempts : typing.Optional[int]
        The maximum number of reconnection attempts for resumable streaming endpoints. Defaults to no limit. Per-request `max_stream_reconnection_attempts` in `request_options` takes precedence over this value.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.AsyncClient]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    logging : typing.Optional[typing.Union[LogConfig, Logger]]
        Configure logging for the SDK. Accepts a LogConfig dict with 'level' (debug/info/warn/error), 'logger' (custom logger implementation), and 'silent' (boolean, defaults to True) fields. You can also pass a pre-configured Logger instance.

    Examples
    --------
    from leal import AsyncLeal

    client = AsyncLeal(
        token="YOUR_TOKEN",
    )
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: LealEnvironment = LealEnvironment.PRODUCTION,
        token: typing.Union[str, typing.Callable[[], str]],
        headers: typing.Optional[typing.Dict[str, str]] = None,
        async_token: typing.Optional[typing.Callable[[], typing.Awaitable[str]]] = None,
        timeout: typing.Optional[float] = None,
        max_retries: typing.Optional[int] = None,
        stream_reconnection_enabled: typing.Optional[bool] = None,
        max_stream_reconnection_attempts: typing.Optional[int] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.AsyncClient] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
    ):
        _defaulted_timeout = timeout if timeout is not None else 60 if httpx_client is None else None
        _defaulted_max_retries = max_retries if max_retries is not None else 2
        self._client_wrapper = AsyncClientWrapper(
            base_url=_get_base_url(base_url=base_url, environment=environment),
            token=token,
            headers=headers,
            async_token=async_token,
            httpx_client=httpx_client
            if httpx_client is not None
            else _make_default_async_client(timeout=_defaulted_timeout, follow_redirects=follow_redirects),
            timeout=_defaulted_timeout,
            max_retries=_defaulted_max_retries,
            stream_reconnection_enabled=stream_reconnection_enabled,
            max_stream_reconnection_attempts=max_stream_reconnection_attempts,
            logging=logging,
        )
        self._stores: typing.Optional[AsyncStoresClient] = None
        self._cards: typing.Optional[AsyncCardsClient] = None
        self._customers: typing.Optional[AsyncCustomersClient] = None
        self._customer_cards: typing.Optional[AsyncCustomerCardsClient] = None
        self._locations: typing.Optional[AsyncLocationsClient] = None
        self._posters: typing.Optional[AsyncPostersClient] = None
        self._rewards: typing.Optional[AsyncRewardsClient] = None
        self._status: typing.Optional[AsyncStatusClient] = None

    @property
    def stores(self):
        if self._stores is None:
            from .stores.client import AsyncStoresClient  # noqa: E402

            self._stores = AsyncStoresClient(client_wrapper=self._client_wrapper)
        return self._stores

    @property
    def cards(self):
        if self._cards is None:
            from .cards.client import AsyncCardsClient  # noqa: E402

            self._cards = AsyncCardsClient(client_wrapper=self._client_wrapper)
        return self._cards

    @property
    def customers(self):
        if self._customers is None:
            from .customers.client import AsyncCustomersClient  # noqa: E402

            self._customers = AsyncCustomersClient(client_wrapper=self._client_wrapper)
        return self._customers

    @property
    def customer_cards(self):
        if self._customer_cards is None:
            from .customer_cards.client import AsyncCustomerCardsClient  # noqa: E402

            self._customer_cards = AsyncCustomerCardsClient(client_wrapper=self._client_wrapper)
        return self._customer_cards

    @property
    def locations(self):
        if self._locations is None:
            from .locations.client import AsyncLocationsClient  # noqa: E402

            self._locations = AsyncLocationsClient(client_wrapper=self._client_wrapper)
        return self._locations

    @property
    def posters(self):
        if self._posters is None:
            from .posters.client import AsyncPostersClient  # noqa: E402

            self._posters = AsyncPostersClient(client_wrapper=self._client_wrapper)
        return self._posters

    @property
    def rewards(self):
        if self._rewards is None:
            from .rewards.client import AsyncRewardsClient  # noqa: E402

            self._rewards = AsyncRewardsClient(client_wrapper=self._client_wrapper)
        return self._rewards

    @property
    def status(self):
        if self._status is None:
            from .status.client import AsyncStatusClient  # noqa: E402

            self._status = AsyncStatusClient(client_wrapper=self._client_wrapper)
        return self._status


def _get_base_url(*, base_url: typing.Optional[str] = None, environment: LealEnvironment) -> str:
    if base_url is not None:
        return base_url
    elif environment is not None:
        return environment.value
    else:
        raise Exception("Please pass in either base_url or environment to construct the client")
