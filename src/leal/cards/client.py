# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawCardsClient, RawCardsClient
from .types.create_cards_request_card import CreateCardsRequestCard
from .types.create_cards_response import CreateCardsResponse
from .types.get_cards_response import GetCardsResponse
from .types.list_cards_response_item import ListCardsResponseItem
from .types.update_cards_request_card import UpdateCardsRequestCard
from .types.update_cards_response import UpdateCardsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class CardsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCardsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCardsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCardsClient
        """
        return self._raw_client

    def list(
        self,
        account_id: int,
        *,
        scope: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ListCardsResponseItem]:
        """
        Returns loyalty card templates for the specified store. By default, only
        active (unarchived) cards are returned. Use the `scope` parameter to include
        archived cards.

        Parameters
        ----------
        account_id : int
            Parent store ID

        scope : typing.Optional[str]
            Filter cards by archive status. Default: active only.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ListCardsResponseItem]
            An array of card objects

        Examples
        --------
        from leal import Leal

        client = Leal(
            token="YOUR_TOKEN",
        )
        client.cards.list(
            account_id=1,
        )
        """
        _response = self._raw_client.list(account_id, scope=scope, request_options=request_options)
        return _response.data

    def create(
        self, account_id: int, *, card: CreateCardsRequestCard, request_options: typing.Optional[RequestOptions] = None
    ) -> CreateCardsResponse:
        """
        Creates a new loyalty stamp card template for the store. The card defines the
        visual design (colours, icon, strip) and program rules (stamps required,
        initial stamps).

        Parameters
        ----------
        account_id : int
            Parent store ID

        card : CreateCardsRequestCard

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateCardsResponse
            The newly created card object

        Examples
        --------
        from leal import Leal
        from leal.cards import CreateCardsRequestCard

        client = Leal(
            token="YOUR_TOKEN",
        )
        client.cards.create(
            account_id=1,
            card=CreateCardsRequestCard(
                name="name",
            ),
        )
        """
        _response = self._raw_client.create(account_id, card=card, request_options=request_options)
        return _response.data

    def get(
        self, account_id: int, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetCardsResponse:
        """
        Returns a single loyalty card template by ID, including reward and customer card counts.

        Parameters
        ----------
        account_id : int
            Parent store ID

        id : int
            Card ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetCardsResponse
            A card object

        Examples
        --------
        from leal import Leal

        client = Leal(
            token="YOUR_TOKEN",
        )
        client.cards.get(
            account_id=1,
            id=1,
        )
        """
        _response = self._raw_client.get(account_id, id, request_options=request_options)
        return _response.data

    def update(
        self,
        account_id: int,
        id: int,
        *,
        card: UpdateCardsRequestCard,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateCardsResponse:
        """
        Updates an existing loyalty card template. Only the provided attributes are changed.

        Parameters
        ----------
        account_id : int
            Parent store ID

        id : int
            Card ID

        card : UpdateCardsRequestCard

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateCardsResponse
            The updated card object

        Examples
        --------
        from leal import Leal
        from leal.cards import UpdateCardsRequestCard

        client = Leal(
            token="YOUR_TOKEN",
        )
        client.cards.update(
            account_id=1,
            id=1,
            card=UpdateCardsRequestCard(),
        )
        """
        _response = self._raw_client.update(account_id, id, card=card, request_options=request_options)
        return _response.data


class AsyncCardsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCardsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCardsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCardsClient
        """
        return self._raw_client

    async def list(
        self,
        account_id: int,
        *,
        scope: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ListCardsResponseItem]:
        """
        Returns loyalty card templates for the specified store. By default, only
        active (unarchived) cards are returned. Use the `scope` parameter to include
        archived cards.

        Parameters
        ----------
        account_id : int
            Parent store ID

        scope : typing.Optional[str]
            Filter cards by archive status. Default: active only.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ListCardsResponseItem]
            An array of card objects

        Examples
        --------
        import asyncio

        from leal import AsyncLeal

        client = AsyncLeal(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.cards.list(
                account_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(account_id, scope=scope, request_options=request_options)
        return _response.data

    async def create(
        self, account_id: int, *, card: CreateCardsRequestCard, request_options: typing.Optional[RequestOptions] = None
    ) -> CreateCardsResponse:
        """
        Creates a new loyalty stamp card template for the store. The card defines the
        visual design (colours, icon, strip) and program rules (stamps required,
        initial stamps).

        Parameters
        ----------
        account_id : int
            Parent store ID

        card : CreateCardsRequestCard

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateCardsResponse
            The newly created card object

        Examples
        --------
        import asyncio

        from leal import AsyncLeal
        from leal.cards import CreateCardsRequestCard

        client = AsyncLeal(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.cards.create(
                account_id=1,
                card=CreateCardsRequestCard(
                    name="name",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(account_id, card=card, request_options=request_options)
        return _response.data

    async def get(
        self, account_id: int, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GetCardsResponse:
        """
        Returns a single loyalty card template by ID, including reward and customer card counts.

        Parameters
        ----------
        account_id : int
            Parent store ID

        id : int
            Card ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetCardsResponse
            A card object

        Examples
        --------
        import asyncio

        from leal import AsyncLeal

        client = AsyncLeal(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.cards.get(
                account_id=1,
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(account_id, id, request_options=request_options)
        return _response.data

    async def update(
        self,
        account_id: int,
        id: int,
        *,
        card: UpdateCardsRequestCard,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateCardsResponse:
        """
        Updates an existing loyalty card template. Only the provided attributes are changed.

        Parameters
        ----------
        account_id : int
            Parent store ID

        id : int
            Card ID

        card : UpdateCardsRequestCard

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateCardsResponse
            The updated card object

        Examples
        --------
        import asyncio

        from leal import AsyncLeal
        from leal.cards import UpdateCardsRequestCard

        client = AsyncLeal(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.cards.update(
                account_id=1,
                id=1,
                card=UpdateCardsRequestCard(),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(account_id, id, card=card, request_options=request_options)
        return _response.data
